% \section{the impact of larger Batch Size for action recognition}
% \section{the impact of larger Batch Size for Contrastive learning}
% \section{(domain adaption: the relationship between the size of data and number percentage of trainable weights can be further explored}
% \section{the more advanced image encoder such as FlowFormer (transformer for optical flow) can be explored to be backbone of AFRICAN}
% \section{A more advanced method more showing the fine-grained attention map can be further explored. (not rough one)}
